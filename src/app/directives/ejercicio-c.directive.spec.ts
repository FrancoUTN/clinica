import { EjercicioCDirective } from './ejercicio-c.directive';

describe('EjercicioCDirective', () => {
  it('should create an instance', () => {
    const directive = new EjercicioCDirective();
    expect(directive).toBeTruthy();
  });
});
