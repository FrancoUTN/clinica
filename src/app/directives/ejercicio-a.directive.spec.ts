import { EjercicioADirective } from './ejercicio-a.directive';

describe('EjercicioADirective', () => {
  it('should create an instance', () => {
    const directive = new EjercicioADirective();
    expect(directive).toBeTruthy();
  });
});
