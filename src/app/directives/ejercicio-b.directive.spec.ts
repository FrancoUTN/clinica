import { EjercicioBDirective } from './ejercicio-b.directive';

describe('EjercicioBDirective', () => {
  it('should create an instance', () => {
    const directive = new EjercicioBDirective();
    expect(directive).toBeTruthy();
  });
});
